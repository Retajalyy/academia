import 'package:flutter/material.dart';

class AppColors {
  // Primary & Secondary
  static const Color primaryBlue = Color(0xFF0C4D83); // Main headers, navbar, icons, important text
  static const Color secondaryYellow = Color(0xFFFFC258); // Primary actions, highlights, badges

  // Text Colors
  static const Color primaryText = Color(0xFF0C4D83); // Titles, main info, names
  static const Color darkgrey = Color(0x000ef2f0); // Subtext, descriptions
  static const Color disabledText = Color(0xFFA0A0A0); // Placeholders, inactive fields

  // Accent Colors (for courses)
  static const Color accentProgramming = Color(0xFFFFD600); // Programming course cards/labels
  static const Color accentDataStructures = Color(0xFF4E91F9); // Data Structures course cards/labels
  static const Color accentStatistics = Color(0xFFFF6B81); // Statistics course cards/labels

  // Backgrounds
  static const Color screenBackground = Color(0xFFF5F5F5); // Main background behind cards
  static const Color cardBackground = Color(0xFFFFFFFF); // Cards, popups, content containers
  static const Color navbarFooterBackground = Color(0xFFF0F0F0); // Navbar/Footer background

  // Buttons & Icons
  static const Color disabledButton = Color(0xFFC0C0C0); // Inactive buttons
  static const Color activeIcon = Color(0xFF1E2A5D); // Selected or active icons
  static const Color inactiveIcon = Color(0xFFA0A0A0); // Icons that are not selected

  // Alerts
  static const Color alertError = Color(0xFFFF4C4C); // Errors, warnings, notifications

  // White (used for text or background)
  static const Color lightblue = Color.fromARGB(255, 199, 208, 235);
static const Color bottomNav = Color(0xFF000000);
//schedule part
static const Color darkgrey2 = Color(0xFF6B7280); // ✅ proper grey
static const Color accentProgramming1 = Color(0xFF0C4D83);
 static const Color accentAI = Color(0xFFFFC258);
}